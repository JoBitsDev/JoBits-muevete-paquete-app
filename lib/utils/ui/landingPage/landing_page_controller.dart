import 'package:flutter/material.dart';

class LandingPageController extends StatelessWidget {
  const LandingPageController({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
